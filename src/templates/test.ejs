import React from 'react'
import { render, screen } from '@testing-library/react';

import <%= name %>, { T<%= name %>Props } from './<%= name %>'

const defaultProps = {} as T<%= name %>Props

const setup = (props?: T<%= name %>Props) => {
  const newProps = {
  ...defaultProps,
  ...props
  } as T<%= name %>Props;

  const utils = render(<<%= name %> {...newProps} />);

  // example of how to query an element
  const element = () => screen.queryByText('some element')

  return {
    ...utils,
    element
  }
}

describe('<%= name %>}', () => {
  it('should render', () => {
    const { element } = setup()
    expect(element()).toBeInTheDocument()
  })
})
